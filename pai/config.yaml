name: "PAI (Custom Build)"
slug: "pai_custom"
description: "Paradox Alarm Interface built on HA host"
version: "0.0.1"
startup: services
boot: auto
init: false
host_network: true
arch:
  - aarch64
  - amd64
  - armv7
  - i386

map:
  - config:rw

options: {}
schema: {}

